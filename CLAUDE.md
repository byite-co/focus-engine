# focus-engine
스마트폰 전면 카메라 기반 비집중 상태 측정 엔진. Flutter 앱에 붙는
네이티브 엔진(Android 우선)과 순수 로직 코어로 구성된다.

## 정본
- docs/focus/spec-v0.2.0.md 가 설계 기준선이다. 동결 상태이며 수정하지
  않는다. docs/focus/v0-plan-and-gt.md 는 v0 작업 분해와 GT 검증 규칙.
- 스펙과 지시문·코드가 충돌하면 구현 전에 보고한다. 스펙의 모호한
  부분을 구현으로 메우지 않는다.
- 설계 변경은 CHANGELOG.md 의 v0.2.x 항목으로만 쌓는다.

## 수정 범위
- 세션마다 지시문에 적힌 디렉터리만 수정한다. 다른 경로는 읽기 전용.
- 순수 로직은 core/focus-core 에만 둔다. Android API 의존 금지.

## 데이터 경계 (위반 금지)
- 영상, 이미지 프레임, 원시 랜드마크를 파일·로그·크래시 리포트·
  네트워크로 내보내는 코드를 만들지 않는다.
- 모델·DTO·로그 레코드에 배열형 원시 데이터나 비트맵 필드를 두지
  않는다.
- INTERNET 권한과 네트워크 의존성은 승인 없이 추가하지 않는다.
- 디버그 덤프는 빌드 플래그로만 켜고 릴리스 빌드에 컴파일되지 않게
  한다.

## 시간
- 지속시간·윈도우·상태 전이는 monotonic 시간으로 계산한다.
- 프레임에서 나온 값은 콜백 도착 시각이 아니라 capture timestamp 기준.

## 커밋
- 메시지에 관련 GT 시나리오(T1~T11)나 연구노트 번호(RN-###)를 적는다.
- 레포를 public 으로 바꾸거나 알고리즘 상세를 외부 이슈·gist 에 올리지
  않는다.
