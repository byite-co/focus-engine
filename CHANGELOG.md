# CHANGELOG

v0.2.0 = 설계 기준선(2026-09-19). 본문은 고치지 않는다.
설계 변경은 아래 표에 v0.2.x 항목으로만 쌓는다.

| 버전 | 날짜 | 변경 내용 | 재현 로그 또는 GT 번호 | 연구노트 | 발견·결정자 |
|---|---|---|---|---|---|
| v0.2.1 | 2026-09-19 | (1) 정의 명확화 — 판정 1, 2, 8, 9, 10, 11. ① 초당 레코드 `t_mono_ms` 는 1초 버킷 `[t, t+1000)` 의 시작이고, 버킷은 세션 시작 시각에 정렬하며 버킷이 끝날 때 기록한다. ② "N초 연속" = 조건을 만족한 버킷 N개 연속. 소급이 있는 상태(ABSENT·PRONE·PHONE)는 N번째 레코드에서 확정하고 후보 시작 = 첫 버킷 시작; 소급이 없는 상태(AWAY·PAUSED)는 N+1번째 버킷부터(AWAY 5번째, 장기 이탈 16번째, PAUSED 121번째). ⑧ 검출 지연 = interval 시작 큐부터 raw_state 가 그 interval 의 최종 기대 상태로 처음 바뀐 레코드의 t 까지(1초 양자화). ⑨ flapping 분모 = 기대 상태가 일정한 구간의 채점 초 합; 상태 비율 오차는 기대·측정 각각 INVALID·PAUSED 를 분모에서 뺀 비율, INVALID 비중 차이는 별도 보고. ⑩ lifecycle gap 사유는 기기 층이 `GapReason` 으로 넘긴다; Android 는 APP_SWITCH·SCREEN_LOCK gap 을 내지 않는다(초당 레코드의 PHONE); `GapReason.PROCESS_DEATH` 추가 — interval 없이 마지막 기록 시각으로 세션 종료. ⑪ 10분 "초과" 는 엄격 초과이고 초과하면 interval 없이 진입 시각에 `session_end(LIFECYCLE_GAP_TIMEOUT)`. 2차 판정(남은 가정 7): 대본 GT 는 시작 큐를 세션 시작에 맞춘다 — `start_cue_t_mono_ms == header.t_start_mono_ms`, 모든 큐 시각은 버킷 경계(t_start + 1000·k), 어긋나면 GT 파서가 오류; 관찰 GT(`gt_type` 이 scripted 가 아닌 것)는 큐 시각을 가장 가까운 버킷 경계로 반올림하고 경고. 남은 가정 1(재거치 확정 대기 버킷 = INVALID(redock_pending)), 2(IMU UNKNOWN 은 단계 유지), 4(합격선 수치는 GtRules), 6(엔진 쪽 N+1·후보 리셋은 생성기·규칙 객체에만), 8(precision/recall 분모는 모든 채점 초)은 현재 코드로 확정 | NaiveBaselineEngineTest.thirdMissingBucketConfirmsAbsentWithCandidateStartAtTheFirst, ExpectedStateGeneratorTest.awayGraceIsPresentForFourBucketsAndAwayFromTheFifth·headHiddenLowMotionIsInvalidFor120BucketsAndPausedFromThe121st, GtDiffTest.ratiosExcludeInvalidAndPausedFromTheDenominator·flappingCountsChangesInsideConstantExpectedRuns, StateFinalizerTest.processDeathClosesAtTheLastRecordWithoutAnInterval·exactlyTenMinutesIsStillAGap, GtAlignmentTest; PR #3 | RN-001 | 발견: CC 세션 B (PR #3) / 결정: 김요섭 |
| v0.2.1 | 2026-09-19 | (2) 규칙 보강 — 판정 3, 6, 7, 12. ③ 소급 덮어쓰기 표: ABSENT·PRONE 소급은 raw_state 가 PRESENT, AWAY, INVALID(face_missing_unconfirmed, head_missing, face_unstable)인 초만; PHONE 소급은 위에 더해 INVALID(phone_shake); 어떤 소급도 PHONE, PAUSED, 환경 INVALID(low_light, camera_occluded, fps_low, quality_proxy, redock_pending, recalibration)는 덮지 않는다; 환경 INVALID 버킷은 카메라 기반 후보(ABSENT, PRONE, AWAY 유예, 머리 미검출 저움직임 카운터)를 리셋하고 IMU 집어 듦 후보는 리셋하지 않는다. ⑥ 폰 재거치: 거치 자세에서 벗어난 채 정지(RESTING_OFF_DOCK)하면 집어 듦 확정 여부와 무관하게 그 첫 버킷부터 60초까지 INVALID(redock_pending), 이후 PHONE(소급 없음); 다시 움직이면 7번 규칙으로 돌아가고 타이머는 다음 정지에서 새로 센다; 재거치 확정 = 거치 자세 ±10° 안에서 `redock_stationary_confirm_ms`(2000, 초기값) 연속 정지 또는 탭, 확정 뒤 `recalibration_ms`(20000, 초기값) 동안 INVALID(recalibration); GT 의 `phone_use_on_flat_surface` 구간 시작 = 정지 시작, `phone_redock_recalibrating` 구간이 22000ms 보다 짧으면 GT 파서가 경고. ⑦ 집어 듦: `imu_state` 가 LIFTED 또는 MOVING 인 버킷 3개 연속이면 확정(기울기·분산 분기 모두 3초), 3개 미만이면 그 버킷들은 INVALID(phone_shake). ⑫ 얼굴 검출 비율 2단: `face_missing_max_ratio` 0.2(초기값) 미만이면 G1 "얼굴 미검출" 버킷, `face_present_min_ratio` 0.5 이상이어야 방향(G2) 판정, 사이는 INVALID(face_unstable)·`zone_status = no_head_pose`; NaiveBaselineEngine 의 미검출 기준은 `face_missing_max_ratio`. 2차 판정(남은 가정 3): 재캘리브레이션이 움직임이나 off-dock 정지로 중단되면 `recalibration_aborted` 사건을 남기고, 세션 종료 때 열린 재캘리브레이션도 중단 처리해 `recalibration_start` 가 항상 `recalibration_end` 또는 `recalibration_aborted` 와 짝을 이룬다 | BackdateRulesTest, StateFinalizerTest.overwriteTableIsAppliedPerRawStateAndReason·aBackdatedPhoneIsNotOverwrittenByALaterAbsent, PhoneGateTrackerTest.offDockRestWithoutAnyPickupIsPendingForSixtyBucketsThenPhone·threeMovingBucketsConfirmPickupAtTheThirdWithCandidateStartAtTheFirst·twoDockedBucketsConfirmRedockThenTwentyBucketsRecalibration·pendingRestartsAtTheNextRestAfterMotion, FaceBandTest, NaiveBaselineEngineTest.missingBandUsesFaceMissingMaxRatio, ExpectedStateGeneratorTest.shortRecalibratingIntervalIsAWarning, PhoneGateTrackerTest.motionDuringRecalibrationAbortsItAndRestartsThePickupRule·everyRecalibrationStartPairsWithEndOrAborted·finishAbortsAnOpenRecalibrationOnly; GT-T2, T5a·b·c; PR #3 | RN-001 | 발견: CC 세션 B (PR #3) / 결정: 김요섭 |
| v0.2.1 | 2026-09-19 | (3) 정오표 — 판정 4, 14, 15. ④ 상태는 7개(PAUSED 포함), 우선순위 `PHONE > PAUSED > INVALID(환경) > ABSENT > PRONE > AWAY > PRESENT`; 자동 일시정지 중에도 초당 레코드를 남기고 상태는 PAUSED(소급 없음); 레코드를 남길 수 없는 lifecycle gap 만 interval; "세션 시간에서 뺀다"는 집계 규칙으로 PAUSED·INVALID 를 비율 분모에서 빼고 초 수를 따로 보고 — v0-plan 1장의 "여섯 개" 는 오기. ⑭ PAUSED 가 끝나는 큐 뒤 채점 제외 구간은 6초(반응 3 + 재검출 3), 합격선은 "복귀 큐 뒤 7초 안에 재개"(`resume_within_ms` 7000, 초기값) — v0-plan 7장의 5초는 반응 허용을 빠뜨린 오기. ⑮ 실행 순서 정오: A → B → G(골격) → H → C → D → E → F → G(나머지); PR #3 이 G 골격과 H 를 함께 넣은 것이 맞다 | ModelTest.statePriorityAndSets, ExpectedStateGeneratorTest.cueEndingAPausedIntervalExcludesSixSeconds, PassCriteriaTest.t4bPauseTimingAndResumeWithinSevenSeconds; GT-T4b; PR #3 | RN-001 | 발견: CC 세션 B (PR #3) / 결정: 김요섭 |
| v0.2.1 | 2026-09-19 | (4) 기록 스키마 0.2.1 (`feature_schema_version` "0.2.1", `spec_version` "0.2.0" 유지). second: `invalid_reason`(low_light, camera_occluded, fps_low, quality_proxy, phone_shake, redock_pending, recalibration, face_missing_unconfirmed, head_missing, face_unstable; INVALID 가 아니면 null), `candidate_state`·`candidate_start_mono_ms`(AWAY 유예 포함, away 전용 필드 없음), `events` 사건 목록(pickup_candidate, pickup_confirmed, shake, redock_confirmed{by: orientation\|tap}, redock_pending_timeout, notify_reposition, auto_pause_start, auto_resume, recalibration_start, recalibration_end, zone_added{zone_id}; 각 사건에 t_mono_ms), `screen_state`{ON_UNLOCKED, ON_LOCKED, OFF}·`app_state`{FOREGROUND, BACKGROUND} 분리(앱 기준 PHONE = ON_UNLOCKED AND BACKGROUND), `frames_requested`·`frames_processed`·`frames_dropped`·`max_frame_gap_ms`·`gaps_over_80ms`(`fps_actual` 제거), `torso_match` → `shoulder_visibility_min`·`torso_center_offset_ratio`·`torso_width_ratio`, `head_below_shoulder` → `head_offset_below_shoulder_ratio`(머리 landmark 없으면 null; 일치 여부와 엎드림 조건은 엔진이 ParameterSet 으로 계산), `zone_status`{in_zone, outside, no_head_pose}(`zone_id` 는 in_zone 일 때만). header: `t_start_mono_ms`, `t_start_utc_ms`. `session_end` 줄 정식화: `t_mono_ms, t_utc_ms, reason{USER, LIFECYCLE_GAP_TIMEOUT, PROCESS_DEATH_RECOVERED, UNKNOWN}`. 새 줄 타입 `calibration`(calibration_id, version, t_mono_ms, zones ≤ 3, torso ROI 중심·폭, m0_pose, pose_jitter_floor, m0, jitter_floor, jitter_j_baseline, 거치 중력 벡터 3, 거치 가속도 분산, scene 휘도 기준, bg_tile_texture_baseline 16, bg_tile_mask 16)와 `timebase`(camera_ts_source, camera_to_mono_offset_ns, imu_to_mono_offset_ns; 세션 시작과 1분마다). T7c 는 `invalid_reason == camera_occluded` 초 0, T5b 는 `redock_confirmed` 사건 0회로 proxy 없이 판정. 2차 판정(남은 가정 5): 사건을 입력과 출력으로 나눈다 — 입력(사용자·기기) `user_redock_tap`, `zone_added`; 출력(게이트) `pickup_candidate, pickup_confirmed, shake, redock_confirmed{by}, redock_pending_timeout, notify_reposition, auto_pause_start, auto_resume, recalibration_start/end/aborted`. 재생은 입력 사건만 엔진에 넣고 출력 사건은 다시 계산하며, 다시 계산한 출력 사건을 로그의 출력 사건과 종류·t_mono_ms 로 비교해 리포트(`reproducibility.output_event_mismatches`)에 남긴다; 불일치는 재현성 실패로 센다. `redock_confirmed{by: tap}` 은 `user_redock_tap` 입력이 있을 때만 나온다 | SchemaBoundaryTest.secondRecordFieldsMatchSchema021·sessionHeaderFieldsMatchSpecChapter9PlusStartTimes, JsonlCodecTest.roundTripKeepsEverythingInTimeOrder, PassCriteriaTest.t5bCountsRedockConfirmedEventsAndT5cCountsPhoneSeconds·t7cCountsCameraOccludedSecondsAcrossTheWholeInterval, GtDiffMainTest.sampleT2RunsEndToEnd, ModelTest.eventsAreSplitIntoInputAndOutput, ReplayRunnerTest.ignoresLoggedStatesAndOutputEventsButKeepsInputEvents, GtDiffTest.loggedOutputEventsAreComparedWithTheReplay, PhoneGateTrackerTest.onlyAUserRedockTapInputConfirmsByTap; GT-T5b, T7c, T9, T10; PR #3 | RN-001 | 발견: CC 세션 B (PR #3) / 결정: 김요섭 |
| v0.2.1 | 2026-09-19 | (5) 데이터 경계 명확화. `SecondRecord` 에는 수치 배열을 허용하지 않는다(사건 목록만). `calibration` 줄에 한해 이름이 정해진 고정 길이 수치 목록(zones ≤ 3, dock_gravity_vector 3, bg_tile_texture_baseline 16, bg_tile_mask 16)만 허용한다. 그 밖의 배열·중첩 객체·맵은 어떤 로그 모델에도 두지 않는다 | SchemaBoundaryTest.logModelsCarryOnlyScalarsEnumsStringsAndEventLists·secondRecordHasNoNumericArray·calibrationListsAreNamedAndFixedLength; PR #3 | RN-001 | 발견: CC 세션 B (PR #3) / 결정: 김요섭 |

<!-- 예시: | v0.2.1 | … | G1 torso 오탐 | GT-T8 | RN-004 | … | -->
